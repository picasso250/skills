---
name: everything-file-search
description: 使用 Everything HTTP 服务在本地系统上搜索文件并获取其完整路径。
---

# everything-file-search Skill

This skill allows you to search for files on the local system using the Everything HTTP service and retrieve their full paths.

## Usage

To search for a file, run the following command:

```bash
python /path/to/everything-file-search/search.py rg.exe
```
