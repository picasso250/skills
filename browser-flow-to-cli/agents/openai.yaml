interface:
  display_name: "Browser Flow to CLI"
  short_description: "Turn repeatable web interaction into a CLI skill."
  default_prompt: "Use $browser-flow-to-cli to turn this website workflow into a probe script first, then a reliable CLI."
