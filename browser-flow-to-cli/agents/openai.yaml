interface:
  display_name: "Browser Flow to CLI"
  short_description: "Turn repeatable web interaction into a CLI skill."
  default_prompt: "Use $browser-flow-to-cli to validate this browser workflow in the real environment first, then produce the final CLI."
